# Workspace Guidelines

- **README.md Updates:** Whenever updating the `README.md` with new features or logs, always provide and update the full-length file and folder structure tree. Never abbreviate directories or files (do not use `...`). Always include all previous files and folders in full detail.
