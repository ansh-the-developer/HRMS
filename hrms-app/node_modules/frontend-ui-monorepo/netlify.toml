[build]
  base = "hrms-app"
  command = "npm install && npx turbo run build --filter=frontend-ui-monorepo"
  publish = "apps/frontend-ui-monorepo/dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
