[build]
  base = "hrms-app"
  command = "npm install && cd apps/frontend-ui-monorepo && npm install && npm run build"
  publish = "apps/frontend-ui-monorepo/dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
